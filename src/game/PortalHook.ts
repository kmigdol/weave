/**
 * Vibe Jam portal integration point — stretch item, lives as a stub in
 * WEA-1 so the wiring exists from day one.
 *
 * When WEA-5 implements this, the portal will be a floating glowing
 * rectangle above a lane every ~60s of distance; weaving into it fades
 * the screen to white and redirects to a random jam entry from the
 * registry at https://jam.pieter.com/2026/.
 *
 * Intentionally empty until then.
 */
export class PortalHook {
  // No-op stub. See WEA-5.
}
